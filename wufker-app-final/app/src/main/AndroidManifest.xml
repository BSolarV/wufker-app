<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.messirvedevs.wufker">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WufkerApp">
        <activity android:name=".ProfileEditDetailsActivity"></activity>
        <activity android:name=".SingUpActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapActivity" />
        <activity
            android:name=".InitActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WufkerApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ForoActivity"
            android:theme="@style/Theme.WufkerApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.CHOOSER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ForoDetailActivity"
            android:theme="@style/Theme.WufkerApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.ANSWER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResponderPostActivity"
            android:theme="@style/Theme.WufkerApp.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".CrearPostActivity"
            android:theme="@style/Theme.WufkerApp.NoActionBar" />
        <activity
            android:name=".PostDetailActivity"
            android:theme="@style/Theme.WufkerApp.NoActionBar" />
    </application>

</manifest>